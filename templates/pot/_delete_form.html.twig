<form method="post" action="{{ path('app_pot_delete', {'id': pot.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pot.id) }}">
    <button class="btn">Delete</button>
</form>
