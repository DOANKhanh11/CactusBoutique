{% extends 'base.html.twig' %}

{% block title %}Modifier Terreau{% endblock %}

{% block body %}
    <div class="section-header">
        <h1>🌱 Modifier Terreau</h1>
    </div>

    {{ include('terreau/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_terreau_index') }}" class="btn btn-secondary">Back to list</a>

    {{ include('terreau/_delete_form.html.twig') }}
{% endblock %}
