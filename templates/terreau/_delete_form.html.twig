<form method="post" action="{{ path('app_terreau_delete', {'id': terreau.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ terreau.id) }}">
    <button class="btn">Delete</button>
</form>
